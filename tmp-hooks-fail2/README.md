# Initialized Example Repository

This directory is a fully initialized example project produced by the template package.

Use it to inspect the expected repository shape and to smoke-test the scripts before publishing.
