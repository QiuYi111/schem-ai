# Minimal Initialized Repository

This repository is the clean initialized template used by `scripts/init_project.py`.

It is intentionally parked at phase0 with placeholder artifacts that satisfy the structural contract without implying prior design progress.
